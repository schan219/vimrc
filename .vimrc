colorscheme evening	" my favourite built-in colour scheme
syntax enable	" enable syntax processing
set noexpandtab	" tab key is <TAB>
set tabstop=4	" number of visual spaces per TAB
set shiftwidth=8 " number of characters inserted for indentation
set softtabstop=8	" number of spaces in tab when editing
set number	" show line numbers
set showcmd	" show command in bottom bar
set autoindent	" automatically indent lines
set cursorline	"	highlight current line
set lazyredraw	" redraw only when we need to
set showmatch	" highlight matching braces/parens/brackets

" Searching
set incsearch	" search as characters are entered

" Finding Files
set path+=**
filetype indent on	" load filetype-specific indent files
set wildmenu	" visual autocomplete for command menu

